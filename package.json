{
  "name": "wildtrace",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:packages": "pnpm exec turbo build --filter \"./packages/*\"",
    "postinstall": "pnpm build:packages",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.4.4",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.11.0"
}
